This contains only the AndroidManifest.xml file with required permissions. No source code will be added to this skeleton folder.
Build this as part of AOSP source tree and install it in system/priv-app.
